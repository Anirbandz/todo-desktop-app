<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'TaskFlow' %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar">
        <div class="nav-brand">
            <img src="/images/logo.png" alt="TaskFlow" width="32" height="32">
            TaskFlow
        </div>
        <div class="nav-links">
            <a href="/dashboard" class="nav-link">Dashboard</a>
            <a href="/dashboard/tasks" class="nav-link">Tasks</a>
            <a href="/dashboard/calendar" class="nav-link">Calendar</a>
            <a href="/dashboard/settings" class="nav-link">Settings</a>
            <% if (locals.user) { %>
                <div class="user-profile" onclick="window.location.href='/dashboard/profile'">
                    <img src="<%= user.profilePicture ? user.profilePicture : '/images/logo.png' %>" alt="Profile" class="nav-profile-pic">
                    <span class="nav-username"><%= user.name %></span>
                </div>
            <% } %>
        </div>
    </nav>

    <main class="main-content">
        <%- body %>
    </main>

    <footer class="footer">
        <p>&copy; 2025 TaskFlow Desktop App</p>
    </footer>

    <script src="/js/app.js"></script>
</body>
</html> 